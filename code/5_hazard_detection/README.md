## 5. `HD` (Hazard Detection)
1. Create virtual environment using `venv`. Run:
    1. `python3 -m venv env-hd`
    2. `source env-hd/bin/activate`
2. Install dependencies. Assuming `env-hd` is active, run:
    1. `pip install -r requirements.txt`
